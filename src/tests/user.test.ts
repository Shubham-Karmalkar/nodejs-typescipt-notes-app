// import { describe, expect, test, it, jest } from "@jest/globals";
// import { Request, Response } from "express";
// import { UserController } from "@root/controller/user.controller";

// describe("UserController", () => {
//     const userController = new UserController();
//     describe("getUser", () => {
//         it("should return user data when valid userId is provided", async () => {
//             const req = { params: { emailId: "123" } } as unknown as Request;
//             const res = {
//                 status: jest.fn().mockReturnThis(),
//                 statusCode: jest.fn(),
//                 response: jest.fn(),
//             };

//             expect(res.status).toHaveBeenCalledWith(true);
//             expect(res.statusCode).toHaveBeenCalledWith(200);
//         });
//     });
// });
